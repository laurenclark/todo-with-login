import React from 'react';

function Login() {
    return (
        <form>
            <input type="text" required />
            <button>Login</button>
        </form>
    );
}

export default Login;
